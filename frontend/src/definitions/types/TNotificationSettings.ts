export type TNotificationSettings = {
    receive_new_event_notifications: boolean;
    receive_event_update_notifications: boolean;
    receive_event_reminders: boolean;
    favorite_sports: number[]; // ID видов спорта
};
